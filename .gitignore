node_modules/
.next/
out/
build/

# Entorno — NUNCA se sube: contiene la service_role de Supabase.
.env
.env.local
.env*.local

# Respaldos: llevan datos personales, aunque vayan cifrados.
respaldos/

# Cualquier archivo *.local.* es local y puede contener secretos
# (p. ej. CREDENCIALES-PANEL.local.md).
*.local.md
*.local.txt
*.local.json

# Estado local del CLI de Supabase (contiene el ref del proyecto vinculado).
supabase/.temp/
supabase/.branches/

# Permisos personales de Claude Code. Son de esta maquina, no del proyecto.
.claude/settings.local.json

.DS_Store
*.pem
npm-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts
basedatos.tmp
